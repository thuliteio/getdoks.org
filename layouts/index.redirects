# Redirect default Netlify subdomain to primary domain
https://getdoks.netlify.app/* https://getdoks.org/:splat 301!

# redirects for Netlify - https://www.netlify.com/docs/redirects/
{{- range $p := .Site.Pages -}}
{{- range .Aliases }}
{{ . }} {{ $p.RelPermalink -}}
{{- end }}
{{- end -}}